CC = gcc
SRC = main.c school.c
OBJ = ${src:.c=.o}
NAME = school

all: ${NAME}

${NAME}: ${OBJ}
	${CC} ${OBJ} -o ${NAME}

%.o: %.c
	${CC} -c -o $@ $<

clean:
	${RM} ${NAME}
	${RM} *~ \#*\# *.swp

oclean:
	${RM} ${OBJ}

fclean: clean oclean

re: fclean all

.PHONY: all clean oclean fclean re
